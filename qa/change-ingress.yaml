apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: mmshost-ingress
  namespace: mms-app
  annotations:
    alb.ingress.kubernetes.io/tags: Environment=QA,Env=QA,ApplicationName=mms-app
spec:
  rules:
    - host: qa-mms.my-domain.com
      http:
        paths:
          - path: /*
            backend:
              serviceName: mmshost
              servicePort: 8086
